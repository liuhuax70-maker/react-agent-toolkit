# 示例任务

可直接粘贴到界面输入框，或作为 `/api/v1/agent/run` 的 `question`。

1. 现在几点？然后用计算器算出 (128+372)*15/4，把结果写入 result.txt
2. 统计这句话的词数：今天天气很好，适合出门散步
3. 先看看工作目录里有哪些文件，再读取 note.txt 的内容并告诉我它有多少行
4. 计算 2 的 20 次方，并说明这个数字的量级
5. 搜索 LangGraph ReAct，总结前三条结果的标题

带工具调用的任务最能体现智能体的能力；纯闲聊类问题不会触发工具。
